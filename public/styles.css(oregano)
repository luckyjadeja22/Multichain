/* ============================================
   Professional Blockchain Pulse Dashboard
   Modern, Responsive Design
   ============================================ */

/* === CSS Variables for Easy Theming === */
:root {
  --bg-primary: #0a0a0f;
  --bg-secondary: #111118;
  --bg-card: #151520;
  --bg-card-hover: #1a1a28;
  --border-color: #252535;
  --border-hover: #353545;
  --accent-primary: #00ff88;
  --accent-secondary: #00cc6a;
  --accent-glow: rgba(0, 255, 136, 0.3);
  --text-primary: #ffffff;
  --text-secondary: #aaaaaa;
  --text-muted: #666666;
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.5);
  --shadow-glow: 0 0 20px var(--accent-glow);
  --gradient-primary: linear-gradient(135deg, #00ff88 0%, #00cc6a 100%);
  --gradient-bg: linear-gradient(180deg, #0a0a0f 0%, #050508 100%);
  --transition-fast: 0.2s ease;
  --transition-normal: 0.3s ease;
  --transition-slow: 0.5s ease;
}

/* === Reset & Base Styles === */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  background: var(--bg-primary);
  background-image: 
    radial-gradient(circle at 20% 50%, rgba(0, 255, 136, 0.05) 0%, transparent 50%),
    radial-gradient(circle at 80% 80%, rgba(0, 255, 136, 0.03) 0%, transparent 50%),
    var(--gradient-bg);
  color: var(--text-primary);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', sans-serif;
  margin: 0;
  padding: 20px;
  min-height: 100vh;
  overflow-x: hidden;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  position: relative;
}

/* === Animated Background Particles === */
body::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: 
    radial-gradient(2px 2px at 20% 30%, rgba(0, 255, 136, 0.15), transparent),
    radial-gradient(2px 2px at 60% 70%, rgba(0, 255, 136, 0.1), transparent),
    radial-gradient(1px 1px at 50% 50%, rgba(0, 255, 136, 0.2), transparent),
    radial-gradient(1px 1px at 80% 10%, rgba(0, 255, 136, 0.1), transparent),
    radial-gradient(2px 2px at 90% 80%, rgba(0, 255, 136, 0.15), transparent),
    radial-gradient(1px 1px at 33% 90%, rgba(0, 255, 136, 0.1), transparent);
  background-size: 200% 200%;
  background-position: 0% 0%, 100% 0%, 100% 100%, 0% 100%, 50% 50%, 0% 0%;
  animation: particleMove 20s ease infinite;
  pointer-events: none;
  z-index: 0;
  opacity: 0.6;
}

@keyframes particleMove {
  0%, 100% {
    background-position: 0% 0%, 100% 0%, 100% 100%, 0% 100%, 50% 50%, 0% 0%;
  }
  25% {
    background-position: 100% 0%, 0% 100%, 0% 0%, 100% 100%, 50% 50%, 50% 50%;
  }
  50% {
    background-position: 100% 100%, 0% 0%, 0% 100%, 100% 0%, 50% 50%, 100% 100%;
  }
  75% {
    background-position: 0% 100%, 100% 100%, 100% 0%, 0% 0%, 50% 50%, 50% 0%;
  }
}

/* Ensure content is above particles */
header, .grid, .controls {
  position: relative;
  z-index: 1;
}

/* === Header Styles === */
header {
  text-align: center;
  margin-bottom: 40px;
  padding: 20px 0;
  position: relative;
}

header::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100px;
  height: 2px;
  background: var(--gradient-primary);
  border-radius: 2px;
}

h1 {
  margin: 0 0 10px;
  font-size: clamp(1.8rem, 5vw, 3rem);
  font-weight: 800;
  letter-spacing: -0.02em;
  background: var(--gradient-primary);
  background-size: 200% 200%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-shadow: 0 0 30px var(--accent-glow);
  animation: fadeInDown 0.6s ease, titleShimmer 3s ease-in-out infinite;
}

@keyframes titleShimmer {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

p {
  margin: 0 0 15px;
  font-size: clamp(0.9rem, 2.5vw, 1.1rem);
  color: var(--text-secondary);
  font-weight: 400;
  animation: fadeIn 0.8s ease;
}

/* === Race Timer === */
#raceTimer {
  font-family: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
  font-size: clamp(14px, 3vw, 20px);
  color: var(--accent-primary);
  margin: 20px 0;
  text-align: center;
  font-weight: 700;
  letter-spacing: 0.05em;
  text-shadow: 0 0 10px var(--accent-glow);
  padding: 12px 24px;
  background: rgba(0, 255, 136, 0.1);
  border: 1px solid rgba(0, 255, 136, 0.2);
  border-radius: 12px;
  display: inline-block;
  backdrop-filter: blur(10px);
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(0, 255, 136, 0.4);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(0, 255, 136, 0);
  }
}

/* === Grid Layout === */
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 24px;
  width: 100%;
  max-width: 1600px;
  margin: 0 auto;
  padding: 20px 0;
}

/* Desktop: Force 3 columns for optimal layout */
@media (min-width: 1024px) {
  .grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }
}

/* === Card Styles === */
.card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 16px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  min-height: 280px;
  transition: all var(--transition-normal);
  box-shadow: var(--shadow-md);
  position: relative;
  animation: fadeInUp 0.6s ease backwards;
}

.card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: var(--gradient-primary);
  opacity: 0;
  transition: opacity var(--transition-normal);
}

.card:hover {
  transform: translateY(-4px);
  border-color: var(--border-hover);
  box-shadow: var(--shadow-lg), var(--shadow-glow);
  background: var(--bg-card-hover);
}

.card:hover::before {
  opacity: 1;
}

.card.active {
  border-color: var(--accent-primary);
  box-shadow: var(--shadow-lg), 0 0 25px var(--accent-glow);
  animation: cardPulse 2s ease-in-out infinite;
}

.card.active::before {
  opacity: 1;
  animation: borderFlow 3s linear infinite;
}

@keyframes cardPulse {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-2px);
  }
}

@keyframes borderFlow {
  0% {
    background-position: 0% 0%;
  }
  100% {
    background-position: 100% 0%;
  }
}

.card.syncing {
  position: relative;
}

.card.syncing::after {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(0, 255, 136, 0.1),
    transparent
  );
  animation: shimmer 2s infinite;
}

@keyframes shimmer {
  0% {
    left: -100%;
  }
  100% {
    left: 100%;
  }
}

.card:nth-child(1) { animation-delay: 0.1s; }
.card:nth-child(2) { animation-delay: 0.2s; }
.card:nth-child(3) { animation-delay: 0.3s; }
.card:nth-child(4) { animation-delay: 0.4s; }
.card:nth-child(5) { animation-delay: 0.5s; }
.card:nth-child(6) { animation-delay: 0.6s; }
.card:nth-child(7) { animation-delay: 0.7s; }
.card:nth-child(8) { animation-delay: 0.8s; }
.card:nth-child(9) { animation-delay: 0.9s; }

/* === Label Section === */
.label {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border-color);
  background: rgba(21, 21, 32, 0.8);
  flex-shrink: 0;
  backdrop-filter: blur(10px);
}

.chain {
  color: var(--accent-primary);
  font-weight: 700;
  font-size: clamp(14px, 2.5vw, 18px);
  margin-bottom: 8px;
  letter-spacing: 0.02em;
  text-transform: uppercase;
  display: flex;
  align-items: center;
  gap: 8px;
}

.status-indicator {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--text-muted);
  margin-right: 8px;
  animation: statusPulse 2s ease-in-out infinite;
  vertical-align: middle;
}

.status-indicator.online {
  background: var(--accent-primary);
  box-shadow: 0 0 8px var(--accent-glow);
  animation: statusPulse 1.5s ease-in-out infinite;
}

.status-indicator.syncing {
  background: #ffaa00;
  animation: statusPulse 1s ease-in-out infinite;
}

@keyframes statusPulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.6;
    transform: scale(0.8);
  }
}

.chain::before {
  content: '▸';
  font-size: 12px;
  opacity: 0.7;
  animation: chainPulse 2s ease-in-out infinite;
}

@keyframes chainPulse {
  0%, 100% {
    opacity: 0.7;
    transform: translateX(0);
  }
  50% {
    opacity: 1;
    transform: translateX(2px);
  }
}

.chain.active::before {
  content: '▶';
  color: var(--accent-primary);
  animation: chainActive 1s ease-in-out infinite;
}

@keyframes chainActive {
  0%, 100% {
    opacity: 1;
    transform: translateX(0) scale(1);
  }
  50% {
    opacity: 0.8;
    transform: translateX(2px) scale(1.1);
  }
}

.stats {
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 12px;
  font-size: clamp(11px, 2vw, 13px);
  font-family: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
  color: var(--text-secondary);
  margin-top: 8px;
}

.stats span {
  display: flex;
  align-items: center;
  gap: 6px;
}

.val {
  color: var(--text-primary);
  font-weight: 600;
  background: rgba(0, 255, 136, 0.1);
  padding: 2px 8px;
  border-radius: 4px;
  border: 1px solid rgba(0, 255, 136, 0.2);
  transition: all var(--transition-fast);
  display: inline-block;
  position: relative;
}

.val.updating {
  animation: valueUpdate 0.5s ease;
  background: rgba(0, 255, 136, 0.2);
  border-color: var(--accent-primary);
  box-shadow: 0 0 8px rgba(0, 255, 136, 0.3);
}

@keyframes valueUpdate {
  0% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.1);
  }
  100% {
    transform: scale(1);
  }
}

/* === Canvas Styles === */
canvas {
  width: 100%;
  flex-grow: 1;
  background: #000000;
  image-rendering: pixelated;
  aspect-ratio: 1/1;
  display: block;
  transition: opacity var(--transition-normal);
}

.card.active canvas {
  box-shadow: inset 0 0 20px rgba(0, 255, 136, 0.1);
}

/* === Controls Section === */
.controls {
  text-align: center;
  margin-top: 40px;
  padding: 20px 0;
}

button {
  background: var(--gradient-primary);
  color: #000000;
  border: none;
  padding: 16px 48px;
  font-size: clamp(14px, 2.5vw, 18px);
  font-weight: 700;
  border-radius: 12px;
  cursor: pointer;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  transition: all var(--transition-normal);
  box-shadow: var(--shadow-md), 0 0 20px rgba(0, 255, 136, 0.3);
  position: relative;
  overflow: hidden;
  font-family: inherit;
}

button::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

button:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: var(--shadow-lg), 0 0 30px rgba(0, 255, 136, 0.5);
}

button:hover::before {
  width: 300px;
  height: 300px;
}

button:active {
  transform: translateY(0) scale(0.98);
}

button:focus {
  outline: 2px solid var(--accent-primary);
  outline-offset: 4px;
}

/* === Animations === */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeInDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* === Mobile Optimizations === */
@media (max-width: 768px) {
  body {
    padding: 15px;
  }
  
  header {
    margin-bottom: 30px;
  }
  
  .grid {
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 16px;
  }
  
  .card {
    min-height: 240px;
  }
  
  .label {
    padding: 12px 16px;
  }
  
  .controls {
    margin-top: 30px;
  }
  
  button {
    padding: 14px 36px;
    width: 100%;
    max-width: 300px;
  }
}

@media (max-width: 480px) {
  .grid {
    grid-template-columns: 1fr 1fr;
    gap: 12px;
  }
  
  .card {
    min-height: 200px;
  }
  
  h1 {
    font-size: 1.5rem;
  }
  
  #raceTimer {
    font-size: 14px;
    padding: 10px 16px;
  }
}

/* === Loading States === */
.card.loading {
  opacity: 0.6;
  pointer-events: none;
}

.card.loading::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 24px;
  height: 24px;
  border: 3px solid var(--border-color);
  border-top-color: var(--accent-primary);
  border-radius: 50%;
  animation: spin 1s linear infinite;
  transform: translate(-50%, -50%);
}

@keyframes spin {
  to {
    transform: translate(-50%, -50%) rotate(360deg);
  }
}

/* === Winner Badge Styles === */
.stats div[style*="color: #00ff88"] {
  margin-top: 8px;
  padding: 6px 12px;
  background: rgba(0, 255, 136, 0.15);
  border: 1px solid rgba(0, 255, 136, 0.3);
  border-radius: 8px;
  animation: winnerPulse 1.5s ease-in-out infinite;
}

@keyframes winnerPulse {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(0, 255, 136, 0.4);
  }
  50% {
    box-shadow: 0 0 0 4px rgba(0, 255, 136, 0);
  }
}

@keyframes celebration {
  0%, 100% {
    transform: scale(1) rotate(0deg);
  }
  25% {
    transform: scale(1.05) rotate(-2deg);
  }
  75% {
    transform: scale(1.05) rotate(2deg);
  }
}

.winner-badge {
  display: inline-block;
  animation: winnerPulse 1.5s ease-in-out infinite, fadeInUp 0.5s ease;
}

/* === Scrollbar Styling === */
::-webkit-scrollbar {
  width: 10px;
}

::-webkit-scrollbar-track {
  background: var(--bg-primary);
}

::-webkit-scrollbar-thumb {
  background: var(--border-color);
  border-radius: 5px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--border-hover);
}

/* === Accessibility === */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* === Print Styles === */
@media print {
  body {
    background: white;
    color: black;
  }
  
  .card {
    border: 1px solid #ccc;
    break-inside: avoid;
  }
  
  button {
    display: none;
  }
}

